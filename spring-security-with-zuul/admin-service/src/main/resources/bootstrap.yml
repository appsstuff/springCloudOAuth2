server:
  port: 8081

spring:
  application:
    name: admin-service

  cloud:
    config:
      name: admin-service
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"
